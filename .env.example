# Mkweli AML Screening System - Environment Configuration
# Copy this file to .env and configure your settings

# =============================================================================
# SECURITY SETTINGS
# =============================================================================

# Flask secret key for session encryption
# IMPORTANT: Generate a secure random key for production!
# Linux/Mac: python3 -c "import secrets; print(secrets.token_hex(32))"
# Windows: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secure-secret-key-here-change-me

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Environment mode: production or development
FLASK_ENV=production

# Debug mode (set to false in production)
FLASK_DEBUG=false

# =============================================================================
# SERVER SETTINGS
# =============================================================================

# Port to run the application (default: 8000)
PORT=8000

# Number of Gunicorn workers (recommended: 2-4 × CPU cores)
WORKERS=4

# =============================================================================
# DOCKER SETTINGS (used by docker-compose)
# =============================================================================

# Container name
CONTAINER_NAME=mkweli-aml

# =============================================================================
# NOTES
# =============================================================================

# 1. Never commit this file with actual secrets to version control
# 2. Use strong, unique SECRET_KEY in production
# 3. Adjust WORKERS based on your server's CPU cores
# 4. Port 8000 is recommended for production (avoid 5000 in production)
